# CLAUDE.md

Canonical AI repository instructions live in `AGENTS.md` and `.github/instructions/project.instructions.md`.

Use those files for the project purpose, stack, repo map, working rules, verification commands, and links into `agent_docs/`.

Keep this file as a redirect only so the top-level guidance stays concise and does not drift across multiple copies.

